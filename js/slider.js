var slider = tns({
    container: ".homeBanner__slider",
    items: 1,
    speed: 800,
    gutter: 100,
    slideBy: "page",
    autoplay: true,
    navPosition: "bottom",
    axis: "vertical",
    // containerControls: "#controls",
    // prevButton: ".prev",
    // nextButton: ".next",
  });